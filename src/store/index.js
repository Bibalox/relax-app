import { createStore } from 'vuex'

export default createStore({
  state: {
    soundEnabled: false,
    vibrationEnabled: false,
    musicEnabled: false,
    breathingDuration: 10000
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
